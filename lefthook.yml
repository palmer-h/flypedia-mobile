pre-commit:
  parallel: true
  commands:
    typecheck:
      run: yarn typecheck
    lint:
      run: yarn lint

commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
