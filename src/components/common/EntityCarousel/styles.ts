import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  title: {
    marginBottom: 8,
  },
  itemContainer: {
    alignItems: 'center',
    width: 128,
    padding: 8,
  },
  itemTitle: {
    textAlign: 'center',
    marginTop: 8,
  },
});
