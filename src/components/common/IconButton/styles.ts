import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderRadius: 100,
    padding: 6,
  },
  containerShadow: {
    elevation: 4,
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.3,
    shadowRadius: 2,
  },
});
