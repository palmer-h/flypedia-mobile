import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: '100%',
    alignItems: 'center',
  },
  loadingIndicator: {
    marginTop: 'auto',
    marginBottom: 'auto',
  },
});
